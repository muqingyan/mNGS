[version]
version = ${version}

[scripts]
scripts = {scripts_DIR}

[input]
staxid_list = {species_staxid.list }

[output]
DB_dir = {OUT_DIR}

[oldmysql]
host = {host_ip} 
port = 3306
user = username
passwd = passwd
sql_db = mNGS

[newmysql]
host = 10.1.1.104
user = hupf
passwd = HPF321!
sql_db = hpf_demo

[ncbi_summary_file]
taxonomy_dir = {taxonomy}
assembly_summary_genbank = assembly_summary_genbank.txt

[other_file]
fasta_dir = {current_fasta_DIR}
chinese_name = /GPFS01/home/denghj/bin/Microbe_detect/Chinese_name.txt

[params]
threshold = 0
samtools = {samtools}
bowtie2 = {bowtie2-build}

[ncbi_fasta_check_log]
assemblyReport_checklog = assemblyReport.check.log
fasta_checklog = genome.check.log
fasta_assemblyReport_dir = assemblyReport
fasta_genome_dir = genome
fasta_md5_dir = md5


